/*
 *   params: 
 *       - width: Number, the width of the canvas.
 *       - heghit: Number, the height of the canvas.
 *       - rects: Array, a list of Object: {id, weight, ratio}
 *   return:
 *       - layouts: Array, a list of Object: {id, x, y, width, height}
 */

function getLayouts(width, height, rects) {
  const layouts = [];

  // pass

  return layouts;
}

export default getLayouts;
