//   useEffect(()=>{
//       if(type === 'canvas'){
//          const context = document.getElementById(canvasId);
//          const ctx = context.getContext('2d');
//          const code = `(${value})(ctx)`;
//          eval(code)
//       }
//   })

export default function (){
    return <h1>canvas</h1>
}